---
title: "Fixture — a table in a plain Markdown note"
description: "The .md half of the table check: no MDX, no components, same wrapper."
published: 2026-01-07
---

`.md` and `.mdx` share one processor, which is the thing worth asserting rather
than assuming:

| Decides what to do | Decides how to do it | Notes |
| --- | --- | --- |
| caller | tool | a reasonably wide third column so the table has to scroll |
